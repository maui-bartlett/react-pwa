# Backend Development Progress

## Current branch

- `feat/convex-auth-backend`
- Created from `master` on 2026-05-19 Hawaii time.

## User-directed prerequisite

Before continuing with `backend-dev-plan.md`, the user asked that status effects, backstory prompts, and character notes all be part of `characterState`.

Completed:

- `backstoryPrompts` and `notes` were already fields on `Character` and already mutated via `setCharacter`.
- Moved status effects into `Character` as `character.statusEffects`.
- Removed the separate `statusEffectsState` atom from FabU usage.
- Added migration from the old `fab-u-status-effects` localStorage key into `character.statusEffects`.
- Future character writes remove the old standalone `fab-u-status-effects` key.
- Updated `FabU.tsx` to toggle and clear status effects through `setCharacter`.
- Renamed the character currency field and test selectors from `zennit` to `zenit` across code and E2E tests. Old saved localStorage data is still migrated into `character.zenit`.

Validation after this prerequisite:

- `npm run ts:check` passed.
- `npm run prettier:check` passed after formatting `src/pages/FabU/atoms.ts` and `src/pages/FabU/FabU.tsx`.
- `npm run lint:check` passed.
- `npm run test:unit -- --run` passed after adding FabU character migration tests.

## Phase 1 progress

Completed:

- Added `src/domain/fabU/characterTypes.ts`.
- Added `src/domain/fabU/characterDefaults.ts`.
- Added `src/domain/fabU/characterMigration.ts`.
- Recreated `src/pages/FabU/atoms.ts` as a storage adapter around the domain helpers.
- Added `PersistedCharacterState` with `schemaVersion` and `character`.
- Added helpers:
  - `createDefaultCharacter()`
  - `normalizeCharacter(raw)`
  - `migrateCharacter(raw)`
  - `serializeCharacterForBackend(character)`
  - `deserializeCharacterFromBackend(raw)`
- Added `src/domain/fabU/characterMigration.spec.ts`.

Important context:

- `statusEffects`, `backstoryPrompts`, `notes`, and `zenit` are all part of `Character`.
- Backend serialization currently also includes a top-level `statusEffects` mirror for compatibility with the planned Convex schema, but the source of truth remains `character.statusEffects`.
- `src/pages/FabU/atoms.ts` still stores the local character under `fab-u-character` for offline support.

## Phase 0 notes so far

- Initial branch state had untracked files/directories not created by this agent:
  - `.claire/`
  - `.claude/launch.json`
  - `.claude/worktrees/`
  - `.cursor/`
  - `e2e/fab-u/bond-measure.spec.ts`
- Durable FabU state references found before the status-effects consolidation:
  - `src/pages/FabU/atoms.ts`
  - `src/pages/FabU/FabU.tsx`
  - `src/pages/FabU/useCharacterHistory.ts`
  - `src/theme/atoms.ts` for theme mode only.
- `src/pages/FabU/useCharacterHistory.ts` wraps `characterState` and provides undo/redo for all `setCharacter` mutations.

## Next steps

## Phase 2 progress

Completed:

- Installed `convex`.
- Ran hosted Convex setup with project name `fab-u`.
- Dev deployment: `polite-rooster-646`.
- Dashboard URL reported by CLI: `https://dashboard.convex.dev/d/polite-rooster-646`.
- `.env.local` was generated by Convex and contains:
  - `CONVEX_DEPLOYMENT=dev:polite-rooster-646`
  - `VITE_CONVEX_URL=https://polite-rooster-646.convex.cloud`
  - `VITE_CONVEX_SITE_URL=https://polite-rooster-646.convex.site`
- Updated `env/.shared` with public Convex env variable names.
- Set hosted Convex env:
  - `BETTER_AUTH_SECRET`
  - `SITE_URL=http://localhost:5173`
- Added Convex generated files under `convex/_generated/`.

Important context:

- The secret value itself is not recorded here.
- If Vite runs on a fallback port, Better Auth CORS will reject that origin until `SITE_URL` is updated. The intended local app URL remains `http://localhost:5173`.

## Phase 3 progress

Completed:

- Added `src/lib/convexClient.ts`.
- Added `src/lib/auth-client.ts`.
- Wrapped the app in `ConvexBetterAuthProvider` in `src/root.tsx` while preserving Jotai, theme, and Toolpad notification providers.
- Added Vite env typings for Convex public env vars.
- Added `src/components/fab-u/organisms/AccountMenu.tsx` as the first frontend account surface.
- Added a fixed Fab U account entry button and dialog with:
  - email/password sign in
  - email/password account creation
  - magic link request
  - Google OAuth start
  - Discord OAuth start
  - Apple OAuth start
  - signed-in account summary
  - sign out
- Moved the account entry into the mobile app shell as a settings gear in the green header.
- Moved theme switching into the settings dialog.
- Added immediate auth status feedback for sign in, sign up, magic link requests, OAuth starts, and sign out.
- Added Better Auth result-error handling for cases where the client returns `{ error }` instead of throwing.
- Increased the green header's top padding for iOS notch/safe-area breathing room.
- Changed the light app background to a warm paper color and made the shell canvas a slightly lighter paper tone.

## Phase 4 progress

Completed:

- Installed `better-auth` and `@convex-dev/better-auth`.
- Added `convex/auth.config.ts`.
- Added `convex/convex.config.ts`.
- Added `convex/auth.ts` with:
  - email/password enabled
  - magic link plugin configured
  - Google OAuth env-gated
  - Discord OAuth env-gated
  - Apple OAuth env-gated
  - Convex and cross-domain plugins for Vite SPA support
- Added `convex/http.ts` to register Better Auth routes with CORS enabled.
- Added `src/lib/auth-client.ts` with Better Auth React client plugins.

Still required from user/operator before real auth smoke testing:

- `AUTH_EMAIL_WEBHOOK_URL` or a real email provider integration for magic links. Without it, magic link URLs are logged server-side only.
- `GOOGLE_CLIENT_ID` and `GOOGLE_CLIENT_SECRET`.
- `DISCORD_CLIENT_ID` and `DISCORD_CLIENT_SECRET`.
- `APPLE_CLIENT_ID` and `APPLE_CLIENT_SECRET` or the Apple-specific secret material needed to produce that secret.
- OAuth redirect URIs should use the Convex site origin, for example `https://polite-rooster-646.convex.site/api/auth/callback/google`.

## Phase 5 progress

Completed:

- Added `convex/schema.ts` with app-owned tables:
  - `userProfiles`
  - `userSettings`
  - `characters`
  - `campaigns`
  - `campaignMembers`
  - `campaignCharacters`
  - `campaignJoinCodes`
  - `campaignInvites`
- Auth-owned tables are provided by the Better Auth Convex component and are not duplicated in the app schema.

## Phase 6 and 7 progress

Completed first-pass backend APIs:

- Added `convex/lib/auth.ts` with reusable auth/profile/permission helpers.
- Added `convex/users.ts`.
- Added `convex/characters.ts`.
- Added `convex/campaigns.ts`.
- Added `convex/invites.ts`.
- Added `convex/lib/hash.ts` for hashed join codes/invite tokens.

Important context:

- Join codes are returned in plaintext only once from `createJoinCode`; only SHA-256 hashes are stored.
- Player accounts, characters, and campaigns use soft archive behavior.
- Removed members and campaign-character links are marked removed rather than hard-deleted in these first-pass APIs.
- Invite revocation currently soft-marks the invite as `revoked`; the plan allows true deletion later as cleanup work.
- `listMyInvites` currently lists account-targeted invites. Email-matching invites after sign-in still need a verified-email matching flow once auth email verification/provider data is fully configured.

## Latest validation

Passed:

- `npx convex codegen`
- `npm run ts:check`
- `npm run lint:check`
- `npm run prettier:check`
- `npm run test:unit -- --run`
- `npm run build`
- `npm run test:e2e -- e2e/fab-u/account-flow.spec.ts --project=chromium`
- `curl` preflight to `https://polite-rooster-646.convex.site/api/auth/get-session` now returns CORS headers for `http://localhost:5173`.

Browser smoke:

- Vite rendered the FabU page successfully on fallback port `http://localhost:5181/`.
- Better Auth session fetch failed CORS there because hosted Convex `SITE_URL` is configured for `http://localhost:5173`, which is expected for that fallback-port test.
- Added `e2e/fab-u/account-flow.spec.ts` for the account dialog mode-switching smoke test.
- Visual smoke at `430x900` confirmed the settings gear stays inside the mobile shell/header and no browser console errors remain.

## Resume point

Resume `backend-dev-plan.md` at Phase 8:

1. Add frontend auth screens/session hooks.
2. Add signed-in character selector.
3. Add `useFabUCharacterPersistence` to bridge Jotai/localStorage with Convex.
4. Add local character claim/import UI.
5. Add debounced backend saves and conflict handling.
6. Add offline sync queue.
